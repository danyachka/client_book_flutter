

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color.fromARGB(255, 55, 189, 129);
  static const primaryTrans = Color.fromARGB(170, 55, 189, 129);

  static const primaryDarker = Color.fromARGB(255, 62, 134, 103);
  static const primaryDark = Color.fromARGB(255, 48, 95, 73);
  static const primaryDarkTrans = Color.fromARGB(120, 48, 95, 73);

  static const primaryLighter = Color.fromARGB(255, 59, 185, 129);
  static const primaryLight = Color.fromARGB(255, 117, 219, 173);
  static const primaryLightTrans = Color.fromARGB(170, 117, 219, 173);

  static const accentText = primary;
  static const accentTextLighter = primaryLight;
  static const accentTextDarker = primaryDarker;
  static const editTextHint = grayLightTrans;

  static const white = Colors.white;
  static const red = Colors.red;

  static const darkBackground = Color(0xFF232323);
  static const darkLighter = Color(0xFF2A2A2A);
  
  static const darkTrans = Color.fromARGB(221, 42, 42, 42);
  static const grayTrans = Color.fromARGB(113, 105, 105, 105);
  static const grayLightTrans = Color.fromARGB(200, 255, 255, 255);

  static const gray = Color(0xFF868686);
  static const grayLight = Color.fromARGB(255, 200, 200, 200);
  static const mediumDarkerGray = Color(0xFF343434);
  static const mediumDarkerGrayTrans = Color.fromARGB(230, 52, 52, 52);
  static const grayDarker = Color(0xFF2E2E2E);
  static const darkGray = Color(0xFF1E1E1E);
}